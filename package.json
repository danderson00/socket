{
  "name": "xsocket",
  "version": "0.0.7",
  "description": "Lightweight observable APIs over any socket",
  "main": "host/index.js",
  "browser": "dist/consumer/index.js",
  "scripts": {
    "build": "babel consumer --presets @babel/preset-env --source-maps both --out-dir dist/consumer && babel common --presets @babel/preset-env --source-maps both --out-dir dist/common",
    "prepublish": "babel consumer --presets @babel/preset-env --source-maps false --out-dir dist/consumer && babel common --presets @babel/preset-env --source-maps false --out-dir dist/common",
    "test": "jest --runInBand"
  },
  "keywords": [],
  "author": "danderson00",
  "license": "MIT",
  "dependencies": {
    "pino": "^5.11.1",
    "uuid": "^3.3.2",
    "verror": "^1.10.0",
    "ws": "^6.1.3",
    "xest": "^0.0.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/preset-env": "^7.3.4",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.4.2",
    "jest": "^24.5.0",
    "pino-pretty": "^2.5.0"
  },
  "resolutions": {
    "write-file-atomic": "2.3.0"
  }
}
